services:
  - type: web
    name: yt-clipper
    env: node
    buildCommand: npm install && node scripts/install-ytdlp.js && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
