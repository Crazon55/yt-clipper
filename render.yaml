services:
  - type: web
    name: yt-clipper
    env: node
    buildCommand: npm install --include=dev && (node scripts/install-ytdlp.js || pip install yt-dlp) && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
